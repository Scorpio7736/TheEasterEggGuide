<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".TheEasterEggGuideApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/app_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.TheEasterEggGuide">
        <activity
            android:name=".MAIN_PAGE"
            android:exported="true"
            android:theme="@style/Theme.App.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HOME_PAGE" />
        <activity android:name=".MAPS_PAGE" />
        <activity android:name=".MAPPEGG_PAGE" />
        <activity android:name=".SETTINGS_PAGE" />
        <activity android:name=".ABOUT_ME_PAGE" />
        <activity android:name=".PROFILE_PAGE" />
        <activity android:name=".Favorites_PAGE"
            android:exported="false" />
        <activity android:name=".EasterEgg_Reveal_Page" />
        <activity android:name=".MusicSettings_PAGE" />
        <activity android:name=".MapRecomend_PAGE" />
        <activity android:name=".VideoPlayer_PAGE" />
        
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.theeastereggguide.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>